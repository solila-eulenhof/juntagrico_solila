[project]
name = "juntagrico-solila"
version = "0.1.0"
requires-python = ">=3.11,<3.12"
dependencies = [
    "gunicorn==22.0.0",
    "juntagrico~=1.6.0",
    "psycopg>=3.1.8",
    "setuptools<81",
]

[dependency-groups]
dev = [
    "psycopg-binary>=3.1.8",
]
